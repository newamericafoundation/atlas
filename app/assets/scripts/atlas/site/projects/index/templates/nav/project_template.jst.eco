<% icon = 'plus' %>
<% icon = if @name is 'Tilemap' then 'map' else icon %>
<% icon = if @name is 'Explainer' then 'dictionary' else icon %>
<% icon = if @name is 'Polling' then 'graph' else icon %>

<div class="icon-button__icon bg-img-<%= icon %>--black">
</div> 
<p class="icon-button__text"><%= if @display_name? then @display_name else @name %></p>