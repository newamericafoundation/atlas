import 'babel-polyfill'

import assert from 'assert'

import { UpdateTracker } from './../tracker.js'


describe('UpdateTracker', () => {

	it('is a class', () => {
		assert.equal(typeof UpdateTracker, 'class')
	})

})